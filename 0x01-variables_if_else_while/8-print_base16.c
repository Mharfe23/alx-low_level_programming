#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - whatever
 * Description:blablabla
 * section header:ldlad
 * Return:0
 */
int main(void)
{
	char c;

	int i;

	for (i = 48;i =<57; i++)
		putchar(i);
	for (c = 'a'; c = 'f'; c++)
		putchar(i);
	putchar('\n');
}
